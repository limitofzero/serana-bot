# Copy to .env and fill in. `.env` is gitignored.

# --- Model backend (any OpenAI-compatible endpoint) ---
SERANA_API_KEY=
SERANA_BASE_URL=https://api.openai.com/v1
SERANA_MODEL=gpt-5

# Optional: a smaller, cheaper model for side tasks (compaction, titles, embeddings).
# Falls back to SERANA_MODEL when unset.
SERANA_AUXILIARY_MODEL=

# --- Telegram frontend ---
# From @BotFather. Only needed for the serana-tg service.
TELEGRAM_BOT_TOKEN=
# Numeric Telegram user ids allowed to talk to the bot, comma separated.
# Leave empty and the bot answers nobody — this is deliberate.
SERANA_ALLOWED_USER_IDS=

# --- Runtime ---
RUST_LOG=info
